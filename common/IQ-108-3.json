{
  "title": "Token Encryption",
  "test_description": "Validate that Personal Access Tokens (PAT) are properly encrypted and not exposed in API responses",
  "version": "1.0",
  "test_code": "IQ-108-3",
  "test_seq": 3,
  "test_suite": "IQ-108",
  "test_suite_title": "Git Connection Validation",
  "category": "IQ",
  "api_calls": [
    {
      "step": 1,
      "name": "list_git_credentials",
      "method": "GET",
      "api_url": "${workspace_url}/api/2.0/git-credentials",
      "headers": {
        "Authorization": "Bearer ${token}",
        "Content-Type": "application/json"
      },
      "payload": {},
      "post_processing": "async function processStep(responseData, currentVariables) { const credentials = responseData?.credentials || []; let credentialsFound = credentials.length > 0; let tokenExposed = false; let targetCredential = null; const gitUsername = currentVariables.git_username; for (let i = 0; i < credentials.length; i++) { const cred = credentials[i]; if (cred && cred.git_username === gitUsername) { targetCredential = cred; if (cred.personal_access_token && cred.personal_access_token.length > 0) { tokenExposed = true; } break; } } if (!targetCredential && credentials.length > 0) { targetCredential = credentials[0]; if (targetCredential.personal_access_token && targetCredential.personal_access_token.length > 0) { tokenExposed = true; } } logger.debug('Token encryption check:', { credentialsFound, credentialsCount: credentials.length, tokenExposed, hasTargetCredential: !!targetCredential }); await delay(3000); logger.debug('Waited 3s after credential check'); return { ...currentVariables, credentials_found: credentialsFound, credentials_count: credentials.length, token_exposed: tokenExposed, target_credential: targetCredential }; }"
    }
  ],
  "response_schema": null,
  "parser_code": "function aggregateResults(stepResults) { const credStep = stepResults.list_git_credentials; const expected_result = 'PAT Storage: Azure Key Vault (AES-256).'; let actual_result = 'PAT encryption not tested'; if (!credStep || credStep.error) { return { status: 'fail', message: 'Failed to get Git credentials', code: 'GIT_CREDENTIALS_FAILED', test_id: 'IQ-108-3', title: 'Token Encryption', expected_result, actual_result }; } const credentials = credStep.data?.credentials || []; const tokenExposed = variables.token_exposed || false; const credentialsFound = variables.credentials_found || false; if (!credentialsFound || credentials.length === 0) { actual_result = 'No Git credentials found'; return { status: 'fail', message: 'No Git credentials found to validate encryption', code: 'NO_CREDENTIALS', test_id: 'IQ-108-3', title: 'Token Encryption', expected_result, actual_result }; } if (!tokenExposed) { actual_result = 'PAT not exposed in credential details'; return { status: 'pass', message: 'PAT properly encrypted - not exposed in API response', code: 'PAT_SECURE', test_id: 'IQ-108-3', title: 'Token Encryption', expected_result, actual_result }; } else { actual_result = 'PAT exposed in credential details'; return { status: 'fail', message: 'PAT exposed in credential details - security risk', code: 'PAT_EXPOSED', test_id: 'IQ-108-3', title: 'Token Encryption', expected_result, actual_result }; } }",
  "manual_procedure": "Check the secret scope configuration for the repository and verify PAT is stored securely."
} 